SERVER_PORT=4000
CLIENT_PORT=5173
OBSERVABILITY_SERVER_URL=http://localhost:4000

# LLM Evaluations (optional — set one or both)
ANTHROPIC_API_KEY=                   # Enables Anthropic provider (Claude Sonnet)
GOOGLE_API_KEY=                      # Enables Gemini provider
EVAL_PROVIDER=                       # Force provider: anthropic | gemini (default: auto-detect)
EVAL_MODEL=gemini-2.5-flash          # Override model (default: per-provider)
